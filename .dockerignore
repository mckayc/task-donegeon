# Git and repository metadata. This isn't needed by the application to run.
.git
.gitignore

# Node.js dependencies. The Dockerfile runs `npm install`, so we don't
# need to copy the massive local node_modules folder.
node_modules/

# IDE and OS-specific files. These are specific to your local machine.
.vscode/
.idea/
.DS_Store
Thumbs.db
# Build output folders. These are generated by the `npm run build` command
# inside the container, so we don't need to copy any old local versions.
dist/
dist-backend/

# Local database files. Your local test data should never be in the image.
*.db
*.db-journal

# Environment variable files. Secrets must be injected at runtime,
# not copied into the image where they could be exposed.
.env
.env.*

# Docker-specific files. The container doesn't need its own recipe.
Dockerfile
docker-compose*.yml
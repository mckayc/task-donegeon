# Git version control files.
.git
.gitignore

# Node.js dependency folders (these are installed inside the container)
node_modules
backend/node_modules
npm-debug.log

# Build output folder (this is generated inside the container)
dist

# Environment files (these should be injected, not copied)
.env
.env.*

# OS-specific files
.DS_Store
Thumbs.db

# Docker-specific files
Dockerfile
docker-compose*.yml
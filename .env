# .env file for Docker Compose

# These values define the user, password, and database name for the PostgreSQL container.
# The 'app' service in docker-compose.yml will use these to build its connection string.
POSTGRES_USER=donegeon_user
POSTGRES_PASSWORD=a_very_strong_and_secret_password
POSTGRES_DB=task_donegeon
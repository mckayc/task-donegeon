# ---------------------------------
# Task Donegeon Environment Variables
# ---------------------------------

# Database Credentials
# The password for your PostgreSQL database.
# MAKE THIS A STRONG, UNIQUE PASSWORD.
POSTGRES_PASSWORD=your_super_secret_db_password

# The name of the database to create. You can leave this as default.
POSTGRES_DB=taskdonegeon

# The username for the database. You can leave this as default.
POSTGRES_USER=donegeon_master

# Full Database Connection String
# This MUST match the user, password, and db name above.
# The hostname MUST be 'db' as that is the service name in the compose file.
DATABASE_URL="postgres://donegeon_master:your_super_secret_db_password@db:5432/taskdonegeon"

# Application Settings
# Set the storage provider to 'local' for Docker/Portainer deployments.
STORAGE_PROVIDER=local

# Google Gemini API Key
# This is required for all AI features. Without it, they will be disabled.
API_KEY=your_google_gemini_api_key_here
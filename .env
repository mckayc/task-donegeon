# -----------------------------------------------------------------
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
#
# This file is used for local development with `npm run dev`
# and the local Docker setup described in the README.
# -----------------------------------------------------------------

# Database URL for the local PostgreSQL Docker container.
# IMPORTANT: Replace 'your_secret_password' with the password you used
# when you ran the 'docker run' command for the database.
DATABASE_URL="postgres://postgres:your_secret_password@localhost:5432/postgres"

# Storage provider for local development.
# Use 'local' to store uploaded media in the `backend/uploads` folder.
STORAGE_PROVIDER="local"

# Your Google Gemini API Key (optional).
# Without this key, the AI features in the app will be disabled.
# Get your key from Google AI Studio.
API_KEY="your_gemini_api_key_here"
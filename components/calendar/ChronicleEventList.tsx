import React from 'react';

// This is an obsolete file that was causing build errors.
// It is now an empty placeholder to allow the project to compile.
const ChronicleEventList: React.FC = () => null;

export default ChronicleEventList;

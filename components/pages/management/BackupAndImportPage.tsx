import React from 'react';

// This component is now obsolete. Its functionality has been merged into
// the new DataManagementPage.tsx to create a unified hub for all data tools.
const BackupAndImportPage: React.FC = () => {
    return (
        <div>
            <p>This page has been moved. Please access data tools from the "Data Management" link in the sidebar.</p>
        </div>
    );
};

export default BackupAndImportPage;